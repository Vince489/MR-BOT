{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Minimal Agent Response Schema",
  "description": "Minimal schema for LLM agent responses, enforcing required fields and preventing hallucinations via constrained decoding. All internal reasoning must be recorded using the recordThought tool before generating a response.",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "description": "Next action to execute (e.g., search, respond)."
    },
    "data": {
      "type": "object",
      "description": "Optional payload for the action (e.g., parameters, results).",
      "additionalProperties": true
    }
  },
  "required": ["action"],
  "additionalProperties": false
}
